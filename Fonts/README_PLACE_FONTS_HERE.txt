Portable font support is enabled.

Place your original files here using exactly:

Handel Gothic Regular (H2).ttf
Conduit ITC Regular (ODST).otf

They are copied into the build/publish output automatically.
Ensemble loads them locally, so Windows font installation is not required.
